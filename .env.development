# Dev-tools scaffold: enables the dev-tools UI during local development.
# This variable is compile-time replaced by Next.js — when absent (production),
# the guarded code branches are eliminated as dead code.
NEXT_PUBLIC_DEV_TOOLS=true
